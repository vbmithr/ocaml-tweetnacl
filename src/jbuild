(jbuild_version 1)

(library
 ((name            tweetnacl)
  (public_name     tweetnacl)
  (libraries       (hex cstruct zarith))
  (c_flags         (-O2 -DED25519_SHA512))
  (c_names         (sha512 monocypher))))
